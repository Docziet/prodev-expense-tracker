"# prodev-expense-tracker" 

# SpendSmart — Expense Tracker

A simple expense tracker built with React + TypeScript and Tailwind CSS.
Features: add/edit/delete expenses, filter by category & month, search, dark mode, localStorage.

## Stack
- React + TypeScript (Vite)
- Tailwind CSS
- LocalStorage for persistence

## Run locally
1. Clone:
   ```bash
   git clone <repo-url>
   cd prodev-expense-tracker
